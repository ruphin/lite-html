export const marker = Math.random()
  .toString(36)
  .substring(2);

export const commentMarker = `comment-${marker}`;
export const nodeMarker = `node-${marker}`;
export const attributeMarker = `attribute-${marker}`;
